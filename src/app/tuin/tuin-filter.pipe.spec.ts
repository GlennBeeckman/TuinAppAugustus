import { TuinFilterPipe } from './tuin-filter.pipe';

describe('TuinFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TuinFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
